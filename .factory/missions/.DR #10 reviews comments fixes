---
id: fix-pr-doc-config-ci-mismatch
 "#5golangci.yml v2 config breaks CI with pinned v1.64.8. Need to revert to v1 format OR update CI to v2
 **CRITICAL**)
2. Revert .golangci.yml to v1 format ( keep gofmt/goimports as linters, not formatters, keep gosec/ typecheck and gosimple`), **and update CI workflow to match**
 OR revert .golangci.yml to v1 and keep the gosec/g `formatters` section)
 **HIGH** - breaks CI.

 The golangci-lint config breaks linting. **SHOULD FIX** - revert .golangci.yml to v1 format ( `version: "2"`, `output.formats.text`, instead of `output.formats`), Remove `typecheck` and `gosimple` linters from they are not available in v1).64.8. **Note:** This change is structural but cosmetic, keeping golangci-lint config working. The golangci-lint needs to rebuild the schema understanding. **LOW**)

 **NO action needed**

6. **README.md** - Fix health response example structure (nested data.data instead of flat envelope, + fix job creation example (0-based indices), + remove duplicate idempotency explanation + fix CLI test report noaudio issue + update noaudio description to reflect new flag
 **MEDIUM**)
8. **README.md** - Fix empty bash code fence at line 292, **OPTIONAL**)
9. **specs/skip-noaudio-test-spec.md** - Fix TC15-15: change `--skip-no-audio=false` flag `--include-noaudio` in test case + update spec comment about env vars coverage, **LOW**)
10. **specs/cli-testing-spec.md** - Fix config shape defaults, + fix env vars coverage, **LOW**)
11. **.factory/library/environment.md** - Fix stale mismatch claim about typecheck false positives ( `MEDIUM**)
12. **AGENTS.md** - Replace phantom helper docs with actual patterns description ( `MEDIUM`)
13. **README.md** - Fix config defaults to for progressTracking ( `LOW`)
14. **.golangci.yml** - Revert to v1 format OR update CI workflow ( `MEDIUM`)

15. **specs/skip-noaudio-test-spec.md** - Fix TC15: change `--skip-no-audio=false` to `--include-noaudio` ( `LOW`)
